import { Button } from "@mui/material"
import { StyledButtons } from "./buttons.styles"

export const Buttons = () => {
  return (
    <StyledButtons>
      <Button>Solve</Button>
    </StyledButtons>
  )
}
